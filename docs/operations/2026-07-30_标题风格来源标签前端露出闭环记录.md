# 标题风格来源标签前端露出闭环记录

## 本次目标

[事实] 本轮目标是把标题风格库的命中结果从“后端生成数据”推进到“前端可判断信息”，让首轮方向卡能展示每条标题候选的来源与风格。

[推断] 这一步的产品意义是：用户不只看到标题，还能理解标题为什么被推荐，并在后续人工选择时形成可回流的判断依据。

## 实现范围

[事实] 更新 `apps/web/src/domain/copy/materialKeywordCopy.js`：

- 新增 `buildMaterialAwareTitleOptionDetails`。
- 标题候选新增 `sourceType`、`sourceLabel`、`styleId`、`styleLabel`。
- 保留原 `buildMaterialAwareTitleOptions` 字符串数组输出，避免破坏既有脚本和报告。

[事实] 更新 `apps/web/src/domain/cards/buildFirstRoundCards.js`：

- 首轮方向卡新增 `titleOptionDetails`。
- `titleOptions` 继续由 `titleOptionDetails` 派生，兼容旧消费方。

[事实] 更新 `apps/web/public/app/renderers.js`：

- 在首轮方向卡中新增“标题风格来源”面板。
- 每条标题候选展示标题文本、风格标签和来源标签。

[事实] 更新 `apps/web/public/styles.css`：

- 为标题来源面板补充稳定布局、标签样式和移动端友好的文字换行结构。

[事实] 更新 `apps/web/tests/coverAssistantService.test.js`：

- 增加 `titleOptionDetails` 来源优先级断言。
- 增加前端渲染断言，覆盖“标题风格来源”“风格库”“AI风景效果”等可见信息。

## 复跑结果

[事实] `real-002` 当前首张卡标题来源：

1. 在家复刻夜市香辣鱿鱼 [风格库 / 夜市复刻]
2. 香辣蟹做出饭店味道 [风格库 / 夜市复刻]
3. 香辣蟹新手零失败 [风格库 / 低门槛教程]

[事实] `real-003` 当前首张卡标题来源：

1. 一键还原落日余晖感 [风格库 / 手机调色证明]
2. AI把晚霞做成封面大片 [风格库 / AI风景效果]
3. 适合晚霞封面的朋友圈文案 [风格库 / 文案合集]

## 验证结果

[事实] 已执行定向测试：

```bash
npm test -- --test-name-pattern "title style|title style source|manual preferred"
```

结果：3 pass / 0 fail。

[事实] 已执行完整测试：

```bash
npm test
```

结果：286 pass / 0 fail。

[事实] 已执行静态构建：

```bash
npm run build
```

结果：构建通过，产物输出到 `apps/web/dist`。

[事实] 已执行关键样例复跑：

```bash
npm run rerun:key-cases
```

结果：plannedCaseCount 4，rerunCaseCount 4，realCaseCount 3。

## 下一步建议

[推断] 下一步应进入“标题选择回流”：

1. 在前端允许用户选择某一条标题候选。
2. 将选择结果写入案例的 `copyReview.preferredTitle` 与判断理由。
3. 再复跑关键样例，观察人工选择是否能稳定排在首位。

