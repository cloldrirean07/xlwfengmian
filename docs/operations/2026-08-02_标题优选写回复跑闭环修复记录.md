# 标题优选写回复跑闭环修复记录

> 记录时间：2026-08-02 12:05 CST

## 本轮目标

验证 `real-002` 与 `real-003` 的人工优选标题写回后，是否能在关键样例复跑中真正进入首轮标题候选，并优先展示。

## 发现的问题

[事实] `real-003` 已写入 `copyReview.preferredTitle`，但首次复跑时 `analysis.fields.copyReview` 为空。

[事实] 生成规则 `buildMaterialAwareTitleOptionDetails` 已支持把 `copyReview.preferredTitle` 放在标题候选首位。

[推断] 问题不在标题生成规则，而在案例规范化阶段：真实案例 JSON 被加载后，`copyReview` 没有被 `validateCaseRecord` 保留下来，导致运行流拿不到人工优选字段。

## 修复范围

- 更新 `apps/web/src/domain/cases/validateCaseRecord.js`
  - 新增 `normalizeCopyReview`。
  - 保留 `copyReview.preferredTitle` 与 `copyReview.titleRationale`。
  - 兼容没有 `copyReview` 的旧案例，默认返回空对象。
- 更新 `apps/web/tests/coverAssistantService.test.js`
  - 新增 `runCaseFlow preserves real case manual preferred title in first-round titles` 测试。
  - 验证 `real-003` 运行流能读取人工优选标题，并将其放在首轮标题候选首位。

## 复跑结果

[事实] 已执行：

```bash
npm run validate:cases
npm run generate:key-case-rerun-plan
npm run rerun:key-cases
```

[事实] 复跑摘要：

- 计划样例数：4
- 实际复跑数：4
- 样例案例数：1
- 真实案例数：3

[事实] `real-002` 首轮标题候选首位：

- `辣炒的味蕾`
- 来源：人工优选 / 人工判断

[事实] `real-003` 首轮标题候选首位：

- `AI把晚霞做成封面大片`
- 来源：人工优选 / 人工判断

## 验证结果

- `npm test -- --test-name-pattern "manual preferred|runCaseFlow preserves real case manual"`：2 pass / 0 fail
- `npm test`：294 pass / 0 fail
- `npm run build`：构建通过

## 下一步建议

[推断] 下一步应把本轮有效变更整理为单独提交：

1. 提交 `validateCaseRecord` 的 `copyReview` 保留修复。
2. 提交新增测试。
3. 视情况决定是否提交本轮复跑产生的报告输出；如果只需要代码闭环，建议不提交大量运行日志。
