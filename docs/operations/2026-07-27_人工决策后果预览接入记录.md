# 人工决策后果预览接入记录

- 时间：2026-07-27 10:42
- 执行模式：PI Engine 维护模式
- 本轮目标：在写回态中补充人工决策后果预览，降低采用或暂不采用推荐确认块时的判断成本。

## 1. Requirement Spec 映射

| 需求边界 | 本轮处理 |
| --- | --- |
| 人工确认前不得进入正式写回 | 仅展示后果预览，不修改决策记录 |
| 决策状态需要可理解 | 将 `adopt-recommended` 和 `reject-recommended` 转译为人工可读后果 |
| 已有状态规则不得改变 | 复用现有校验器中的采用、拒绝和等待状态 |
| 不新增接口契约 | 不修改服务端接口、不新增 API |

## 2. 实现范围

- 更新 `public/app/renderers.js`
  - 在“人工采用进度总览”中新增“人工决策后果预览”。
  - 展示“采用推荐确认块”和“暂不采用推荐确认块”两种结果。
- 更新 `public/styles.css`
  - 增加后果预览卡片、双列信息和长文案换行样式。
- 更新 `tests/coverAssistantService.test.js`
  - 增加后果预览结构、文案和样式断言。

## 3. 安全边界

- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增按钮、不新增操作权限。
- 不改变 `validateManualConfirmationDecision` 的状态判断。

## 4. 当前状态

- 当前真实决策：pending
- 采用推荐确认块：预演后可进入安全预览写入前复查
- 暂不采用推荐确认块：保持正式写回锁定
- 下一步仍需人工确认是否采用推荐确认块。
