# 2026-06-20 二轮反馈映射解释接入记录

## 本次完成

- 给反馈映射增加稳定 `id`
- 二轮结果返回 `ruleMeta`
- 二轮结果返回 `mappingExplanation`
- 前端二轮结果页展示命中映射、规则版本、解释摘要
- 端到端运行记录补充规则版本与命中解释

## 为什么这一步重要

- 二轮优化具备了可解释性和可追踪性
- 后续可以基于映射 ID 做误判复盘和规则修订
- 规则版本开始贯穿首轮分析、二轮修正、运行记录

## 当前验证

- 在 `apps/web` 下运行 `npm test`
- 需确认新增测试和原有测试全部通过

## 后续建议

1. 把二轮解释导出到 Obsidian 验证目录。
2. 做误判样本到规则修订任务的自动归并。
3. 增加用户对解释是否认可的反馈字段。
