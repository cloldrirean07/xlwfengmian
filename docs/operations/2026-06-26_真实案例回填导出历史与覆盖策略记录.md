# 2026-06-26 真实案例回填导出历史与覆盖策略记录

## 本次动作

- 单案例导出前增加旧文件状态判断
- 导出后写入导出日志
- 返回最近导出历史
- 页面导出结果增加覆盖与历史信息
- 补充测试与运行校验

## 本次目的

让真实案例回填导出不只是一条“成功提示”，而是能说明：

- 这次是新建还是覆盖
- 最近导出过几次
- 日志记录存在哪里

## 本次实现结果

当前导出结果会多返回：

- `overwrite`
- `history.latestLogPath`
- `history.recentExports`

对应日志会落到：

- `outputs/obsidian-export-logs/real-case-fill-sheets/<caseId>/`

## 验证

- `npm test`
- `node --input-type=module -e "...exportRealCaseFillSheetToObsidian({ caseId: 'real-001' })..."`
- `node --check public/app/renderers.js`

## 当前影响

这次改动让真实案例导出能力开始具备维护记录和覆盖语义。

后续如果继续推进，更自然的方向是：

- 导出历史详情页
- 覆盖前后差异提示
- 是否允许“新建副本而不是覆盖”的策略选择
