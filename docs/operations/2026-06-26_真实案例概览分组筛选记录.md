# 2026-06-26 真实案例概览分组筛选记录

## 本次动作

- 真实案例概览增加分组筛选状态
- 列表渲染增加分组筛选按钮
- 点击筛选后复用现有刷新链路
- 补充语法校验与运行验证

## 本次目的

让真实案例概览更适合真正跑 `2-3` 条案例时的高频查看，而不是每次都在全部卡片里手动找重点。

## 本次实现结果

当前真实案例概览支持：

- 查看全部
- 按分组筛选

并且筛选逻辑建立在已有的概览优先级与分组规则之上，没有额外复制一套判断逻辑。

## 验证

- `npm test`
- `node --check public/app/createApp.js`
- `node --check public/app/renderers.js`
- `node --input-type=module -e "...listAvailableCases()..."`

## 当前影响

这次改动继续强化了“先做信息结构优化”的路线。

后续如果继续推进，更自然的方向是：

- 概览层增加分组折叠
- 增加只看待处理案例的快捷入口
- 在真实案例数量稍微起来后，再统一讨论 UI 结构重排
