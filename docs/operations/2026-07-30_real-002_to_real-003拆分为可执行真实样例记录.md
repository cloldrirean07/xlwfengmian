# real-002_to_real-003 拆分为可执行真实样例记录

- 记录时间：2026-07-30 10:37
- 项目：AI 封面创意助手
- 本轮目标：把正式写回候选批次 `real-002_to_real-003` 拆成可被代码侧识别、复跑和对比的真实样例。

## 1. 这一步的作用

批次记录只能说明“这一组案例有共性问题”，但规则引擎无法直接运行批次标签。

拆成 `real-002` 与 `real-003` 后，每条案例都能进入以下链路：

- 真实案例索引
- 案例结构校验
- 关键样例复跑计划
- 单条 case run
- 复跑差异报告
- 规则修订任务单下游刷新

这让后续判断从“凭批次感受”变成“逐条样例可验证”。

## 2. 数据来源

- 批量回填工作单：`apps/web/outputs/batch-fill-sheets/real-002_to_real-003/fill-sheet.json`
- 批次试跑记录：`apps/web/outputs/batch-run-records/real-002_to_real-003/run-record.json`
- 正式写回后承接计划：`apps/web/outputs/batch-review-manual-formal-write/formal-write-follow-up-plan/formal-write-follow-up-plan.json`

已验证事实：

- 批次案例数：2
- 案例编号：`real-002`、`real-003`
- 当前状态：均为待回填
- 每条缺失字段：9
- 优先补齐字段：来源链接或截图路径、内容主题、素材描述、内容目标
- 批次人工结论：最卡在准备输入信息时，不知道先补哪项

## 3. 本轮写入

- 新增 `apps/web/data/real-cases/items/real-002.json`
- 新增 `apps/web/data/real-cases/items/real-003.json`
- 更新 `apps/web/data/real-cases/index.json`

写入原则：

- 不伪造真实链接、截图、主题或素材细节。
- 用明确“待补”文本保持结构可运行。
- 在 `evidence.notes` 中记录来源批次和当前缺口。
- 在 `operations.maintenanceTags` 中标记正式写回候选批次和输入准备缺口。

## 4. 已执行命令

- `npm run validate:cases`
- `npm run generate:key-case-rerun-plan`
- `npm run rerun:key-cases`
- `npm run generate:formal-write-follow-up-plan`
- `npm run report:real-case-readiness`
- `npm run report:real-case-maintenance-board`
- `node --test --test-name-pattern "runPlatformCaseBatchReview returns a batch board across platform cases"`
- `npm test`
- `npm run build`

## 5. 当前结果

- 案例结构校验：通过
- 总案例数：4
- 真实案例数：3
- 关键复跑样例：`sample-001 / real-002 / real-003 / real-001`
- 实际复跑样例数：4
- `real-002` 首轮主方向：更清楚重点
- `real-003` 首轮主方向：更清楚重点
- `real-002` 当前负向映射：`neg-content-distance`
- `real-003` 当前负向映射：`neg-content-distance`
- 差异样例数：2
- 差异原因：`real-002` 与 `real-003` 首次进入复跑，之前没有 before 基线
- 平台案例批量复盘：已支持平台笔记待补状态
- 回归验证：`282 pass / 0 fail`
- 构建验证：通过

## 6. 后续建议

下一步不应急着改规则，应优先补齐两条真实样例的 P0 字段：

- 来源链接或截图路径
- 内容主题
- 素材描述
- 内容目标

补完后再重跑关键样例复跑，届时差异才更能说明规则是否需要修订。

## 7. real-002 P0 字段补齐记录

- 记录时间：2026-07-30 11:52
- 当前状态：`real-002` 已补齐 P0 字段，`real-003` 仍待补。

已写入字段：

- 来源截图路径：`apps/web/data/real-cases/assets/real-002/IMG_7959_finder-preview.png`、`apps/web/data/real-cases/assets/real-002/IMG_7955_finder-preview.png`
- 内容主题：用 AI 工具快速做一张小红书封面，从选方向到改标题的完整流程；素材主题为螃蟹和辣炒鱿鱼美食分享。
- 素材描述：两张美食照片，分别呈现螃蟹、红油辣椒、葱段、酱汁，以及辣炒鱿鱼、香菜、辣椒和餐桌环境。
- 内容目标：让用户一眼知道这是一篇 AI 封面制作教程，并愿意点开学习如何快速做出更专业的小红书封面。

产品含义：

- `real-002` 已从“批次候选”推进为“可复跑真实输入”。
- 后续复跑可以观察系统是否稳定给出“美食图如何转成封面标题与视觉重点”的判断。
- 仍需补齐 `real-003`，否则 `real-002_to_real-003` 这个批次还不能完整用于规则修订判断。

复跑后结果：

- 真实案例就绪度：`real-002` 为可进入手动验证（9/9），`real-001` 与 `real-003` 仍待回填。
- 关键样例复跑差异：`real-002` 发生 3 项变化。
- 核心变化：首轮主方向从“更清楚重点”转为“更有冲击力”，说明美食素材信息进入后，系统开始识别食欲冲击型点击机制。
- 下游规则修订项：数量未变化，暂不需要直接改规则。

验证命令：

- `npm run validate:cases`：通过，total 4 / real 3。
- `npm run generate:key-case-rerun-plan`：通过，复跑样例仍为 `sample-001 / real-002 / real-003 / real-001`。
- `npm run rerun:key-cases`：通过，实际复跑 4 条。
- `npm run generate:formal-write-follow-up-plan`：通过。
- `npm run report:real-case-readiness`：通过，ready 1 / pending 2。
- `npm run report:real-case-maintenance-board`：通过，readyHighPriority 1。
- `npm test`：通过，282 pass / 0 fail。
- `npm run build`：通过。
- UI 文案扫描：无命中。

## 8. real-003 P0 字段补齐记录

- 记录时间：2026-07-30 14:53
- 当前状态：`real-002` 与 `real-003` 均已补齐 P0 字段。

已写入字段：

- 来源截图路径：`apps/web/data/real-cases/assets/real-003/IMG_0486.HEIC`、`apps/web/data/real-cases/assets/real-003/IMG_0487.HEIC`、`apps/web/data/real-cases/assets/real-003/IMG_0488.HEIC`
- 内容主题：用 AI 工具快速做一张小红书封面，从标题到封面的构思选择；素材主题为夏日晚霞分享。
- 素材描述：三张傍晚图片，画面包含晚霞、云层和落日，希望通过美景分享获得更多点击、认同和赞美。
- 内容目标：让用户一眼知道这是一篇 AI 封面制作教程，并跟着工具快速做出更专业的小红书封面。

产品含义：

- `real-002_to_real-003` 已从“正式写回候选批次”推进为“两条可复跑真实输入”。
- 后续可以比较美食素材和风景素材在封面方向判断上的差异。
- 下一步重点不再是补 P0，而是复跑后判断：系统方向是否贴合真实素材，若不贴合，再进入规则修订任务单。

复跑后结果：

- 真实案例就绪度：`real-002` 与 `real-003` 均为可进入手动验证（9/9），`real-001` 仍待回填。
- 关键样例复跑差异：`real-003` 发生 3 项变化。
- 核心变化：首轮主方向从“更清楚重点”转为“更让人想点开”，说明晚霞素材信息进入后，系统开始识别风景氛围与好奇点击机制。
- 下游规则修订项：数量未变化，暂不需要直接改规则。

验证命令：

- `npm run validate:cases`：通过，total 4 / real 3。
- `npm run generate:key-case-rerun-plan`：通过。
- `npm run rerun:key-cases`：通过，实际复跑 4 条。
- `npm run generate:formal-write-follow-up-plan`：通过。
- `npm run report:real-case-readiness`：通过，ready 2 / pending 1。
- `npm run report:real-case-maintenance-board`：通过，readyHighPriority 2。
- `npm test`：通过，282 pass / 0 fail。
- `npm run build`：通过。
- UI 文案扫描：无命中。
