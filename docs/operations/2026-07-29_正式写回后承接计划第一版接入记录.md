# 正式写回后承接计划第一版接入记录

- 执行时间：2026-07-29 16:47 CST
- 项目：AI 封面创意助手
- 执行阶段：PI Engine 维护模式 / 写回后承接任务复核
- 执行目标：生成并接入「规则修订任务单 + 关键样例复跑计划」第一版，让正式写回后的下一轮行动从任务卡升级为可接口读取、可页面展示、可命令复跑的承接计划。

## 1. 当前输入

- 正式写回元数据：
  `apps/web/outputs/batch-review-manual-formal-write/batch-review-manual-formal-write.json`
- 写回后验收包：
  `apps/web/outputs/batch-review-manual-formal-write/manual-formal-write-post-execution-acceptance/manual-formal-write-post-execution-acceptance.json`
- PI Engine 执行位点审计：
  `apps/web/outputs/pi-engine-execution-position-audit/pi-engine-execution-position-audit.json`
- 规则修订任务单：
  `apps/web/outputs/reports/rule-revision-task-sheet/rule-revision-task-sheet.json`
- 关键样例复跑计划与报告：
  `apps/web/data/operations/key-case-rerun-plan.generated.json`
  `apps/web/outputs/reports/key-case-rerun/key-case-rerun.json`
  `apps/web/outputs/reports/key-case-rerun/key-case-rerun-diff.json`

## 2. 实现范围

- 新增领域对象：正式写回后承接计划。
- 新增应用层：读取写回、验收、PI Engine、规则任务和复跑报告，生成 JSON 与 Markdown。
- 新增脚本：`npm run generate:formal-write-follow-up-plan`
- 新增接口：`GET /api/formal-write-follow-up-plan`
- 前端接入：写回门禁总览与批次复盘看板展示承接计划。
- 新增测试：领域对象、Markdown、接口接线、样式接线和复盘看板渲染断言。

## 3. 第一版计划结果

- 状态码：`formal-write-follow-up-plan-ready`
- 规则修订：规则修订任务单已生成
- 关键样例复跑：关键样例复跑计划已生成
- 推荐命令链：
  - `npm run report:rule-revision-task-sheet`
  - `npm run generate:key-case-rerun-plan`
  - `npm run rerun:key-cases`
  - `npm run export:obsidian-key-case-rerun`
  - `npm run export:obsidian-key-case-rerun-diff`

## 4. 输出文件

- JSON：
  `apps/web/outputs/batch-review-manual-formal-write/formal-write-follow-up-plan/formal-write-follow-up-plan.json`
- Markdown：
  `apps/web/outputs/batch-review-manual-formal-write/formal-write-follow-up-plan/formal-write-follow-up-plan.md`

## 5. 安全边界

- 未自动修改规则引擎。
- 未自动执行关键样例复跑。
- 未写入 Obsidian。
- 规则修订与关键样例复跑仍保持人工复核边界。

## 6. 结论

正式写回后承接计划第一版已接入项目主链路。后续继续推进时，应从该计划进入规则修订任务单复核，再进入关键样例复跑。
