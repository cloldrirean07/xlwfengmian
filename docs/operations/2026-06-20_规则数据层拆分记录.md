# 2026-06-20 规则数据层拆分记录

## 本次完成

- 新建 `apps/web/data/rules/cover-effect-catalog.json`
- 新建 `apps/web/data/rules/feedback-catalog.json`
- 新建 `src/infrastructure/rules/loadRuleCatalog.js`
- `coverEffectCatalog` 改为从 JSON 读取
- `directionCatalog` 改为由 `coverEffectCatalog` 派生
- `feedbackMappings` 改为从 JSON 读取

## 为什么这一步重要

- 让产品规则从代码逻辑中剥离出来
- 为后续案例回填、反馈词扩展、规则版本化留出空间
- 让 Obsidian 方法论更容易逐步同步进代码数据层

## 当前验证

- 在 `apps/web` 下运行 `npm test`
- 结果：`61 passing, 0 failing`

## 后续建议

1. 给规则 JSON 增加结构校验。
2. 开始把最小案例标注库中的高频反馈词写入反馈映射表。
3. 在前端工作台增加“规则版本/命中规则”展示，方便复盘。
