# 人工决策双选项替换辅助记录

- 时间：2026-07-27 11:05
- 执行模式：PI Engine 维护模式
- 本轮目标：让人工采用操作包同时展示“采用推荐确认块”和“暂不采用推荐确认块”的替换辅助。

## 1. Requirement Spec 映射

| 需求边界 | 本轮处理 |
| --- | --- |
| 决策由人工完成 | 仅展示替换项，不自动修改决策记录 |
| 已有状态规则可被理解 | 将 `adopt-recommended` 与 `reject-recommended` 两种状态转为可复制文本 |
| 不新增状态流转 | 复用现有决策校验器支持的三种状态 |
| 不新增接口契约 | 仅修改前端展示，不修改服务端接口 |

## 2. 实现范围

- 更新 `public/app/renderers.js`
  - 将原“替换项”标题调整为“采用替换项”。
  - 新增“暂不采用替换项”展示。
  - 复制源中同时包含采用方案与暂不采用方案。
- 更新 `public/styles.css`
  - 复用替换项卡片样式，补充暂不采用替换项样式。
- 更新 `tests/coverAssistantService.test.js`
  - 增加暂不采用替换项结构、文案和样式断言。

## 3. 安全边界

- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增按钮、不新增服务端接口。
- 不改变采用预演和采用操作包生成逻辑。

## 4. 当前状态

- 当前真实决策：pending
- 采用替换项：可使用
- 暂不采用替换项：可查看并复制
- 下一步仍需人工确认选择。
