# 准拆页三视图深链恢复闭环记录

## 1. 执行时间

- 日期：2026-07-23
- 阶段：PI Engine 维护模式
- 方案：准拆页三视图深链补齐

## 2. 执行依据

- `docs/architecture/准拆页三视图实施方案_v0.1.md`
- `docs/operations/2026-07-23_准拆页三视图代码实现闭环记录.md`

## 3. 问题判断

上一轮已完成三视图点击切换，但直接访问带 hash 的旧锚点时，目标区块可能仍处于非当前视图中。

需要补齐：

- `#real-case-form` 进入案例维护视图。
- `#review-dashboard-section` 进入复盘驾驶舱并生成看板预览。
- 其它已存在锚点按所在产品视图恢复可见状态。

## 4. 核心改动

- `apps/web/public/app/createApp.js`
  - 新增 hash 到产品视图的解析函数。
  - 新增页面加载与 `hashchange` 时的视图恢复逻辑。
  - 保持原有工作组展开和复盘看板生成逻辑不变。

- `apps/web/tests/coverAssistantService.test.js`
  - 增加深链恢复函数、`hashchange` 监听和初始化恢复断言。

## 5. 边界说明

- 未引入正式路由。
- 未修改 API 契约。
- 未新增业务状态流转。
- 未删除既有锚点和工作组入口。

## 6. 验证结果

- JS 语法检查：通过。
- 现有单元测试：`242 pass / 0 fail`。
- UI 文案扫描：仅命中领域词库中的“你知道吗”，非界面文案。
- 本地服务可达性：`/#real-case-form`、`/#review-dashboard-section` 与 `createApp.js` 均返回 `200`。
- 构建脚本：当前 `apps/web/package.json` 未配置 `build` 脚本。
