# 人工确认决策记录模板生成记录

- 时间：2026-07-26 21:48
- 执行模式：PI Engine 维护模式
- 本轮目标：将“是否采用推荐确认块”沉淀为项目内可校验的决策记录。
- 安全边界：只生成项目内模板和校验报告，不写入 Obsidian，不执行正式写回。

## 1. 背景

[事实] 当前人工确认链路已完成草稿验证、写入前预演和交接包生成。

[事实] 正式写回前仍缺少一份可追踪的人工决策记录，用于区分“待确认”“采用推荐确认块”“暂不采用推荐确认块”。

## 2. 本轮新增

- 新增人工确认决策模板生成脚本。
- 新增人工确认决策校验器。
- 新增默认 `pending` 的项目内决策记录。
- 新增校验报告，明确当前是否允许进入安全预览写入前复查。

## 3. 当前默认状态

- 决策状态：pending
- 当前结论：继续等待人工决策。
- 是否进入安全预览写入前复查：否
- 是否执行正式写回：否

## 4. 后续条件

只有当决策状态被明确改为 `adopt-recommended`，且交接包仍校验通过时，才可进入安全预览写入前复查。
