# 2026-06-26 真实案例概览优先级与分组规则记录

## 本次动作

- 新增真实案例概览规则构造器
- 案例列表增加分组与优先级元信息
- 真实案例概览展示分组标签与数量
- 补充排序与分组测试

## 本次目的

让真实案例概览从“静态列表”继续推进成“有先后顺序的维护入口”。

## 本次实现结果

当前真实案例概览可以直接展示：

- 该案例属于哪个维护分组
- 当前回填状态
- 当前回填完成进度
- 分组总量

## 验证

- `npm test`
- `node --input-type=module -e "...listAvailableCases()..."`
- `node --check public/app/renderers.js`

## 当前影响

这次改动让真实案例概览更适合真正跑 `2-3` 条案例闭环。

后续如果继续推进，更自然的方向是：

- 按分组筛选
- 先只看“优先补写并导出”的案例
- 再在真实高频路径稳定后，专门讨论 UI 如何重构
