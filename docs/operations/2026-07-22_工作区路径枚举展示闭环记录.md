# 工作区路径枚举展示闭环记录

## 基本信息

- 日期：2026-07-22
- 模式：PI Engine 维护模式
- 范围：工作区深化 / 工作区路径展示
- 对应文档：[AI封面创意助手_MVP重做_主路径与工作台重做.md](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/docs/prd/AI封面创意助手_MVP重做_主路径与工作台重做.md)

## PRD 映射

- PRD 3.5 工作区路径：仅支持从枚举值中选择。
- PRD 3.5 工作区路径枚举值：优化现有素材、补内容贴合图、做创意概念图。
- PRD 3.6 工作区路径必须基于当前选中方向展开。

## 本轮实现

- [src/domain/workspace/buildActionWorkspace.js](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/apps/web/src/domain/workspace/buildActionWorkspace.js)
  - `optimize-current` 的展示标题对齐为“优化现有素材”。
  - `search-matched` 的展示标题对齐为“补内容贴合图”。
  - `concept-first` 的展示标题对齐为“做创意概念图”。
- [tests/coverAssistantService.test.js](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/apps/web/tests/coverAssistantService.test.js)
  - 新增 `createAnalysisSession exposes PRD workspace path enum labels`，覆盖三类工作区路径展示。

## 验收结果

- `node --check apps/web/src/domain/workspace/buildActionWorkspace.js`：通过。
- `node --check apps/web/tests/coverAssistantService.test.js`：通过。
- `node --test --test-reporter=dot`：通过，205 个测试点全部通过。
- `node -p "Boolean(require('./package.json').scripts.build) ? require('./package.json').scripts.build : 'NO_BUILD_SCRIPT'"`：确认当前 web 包未配置 build 脚本。

## 边界说明

- 本轮只对齐工作区路径展示标题，不改变内部 `workspaceId`。
- 输入 schema、工作区建议生成和二轮承接逻辑保持不变。
- 不新增工作区路径，不扩展 PRD 以外的业务能力。
