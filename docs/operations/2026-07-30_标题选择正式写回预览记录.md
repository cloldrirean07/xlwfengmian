# 标题选择正式写回预览记录

## 本次目标

[事实] 本轮目标是在“标题选择回流闭环第一版”的基础上，补齐正式写回前的预览层：选择标题后，不只生成 `copyReviewDraft`，还展示 `copyReview` 字段的当前值、拟写入值和安全边界。

[推断] 这一步的产品意义是把人工选择从“页面状态”推进到“可审查 patch”，为后续正式写入 `real-002 / real-003` 案例文件做准备。

## 实现范围

[事实] 更新 `apps/web/public/app/createApp.js`：

- 新增 `buildTitleWritebackPreview`。
- 写回预览包含 `mode: preview-only`、`targetPath: copyReview`、`statusLabel`、`safetyNote`、`patchFields` 和 `nextCopyReview`。
- `buildTitleSelectionDraft` 会把 `currentCopyReview` 与标题选择结果合成为 `writebackPreview`。

[事实] 更新 `apps/web/public/app/renderers.js`：

- “人工优选草稿”下方新增“正式写回预览”区域。
- 展示 `copyReview.preferredTitle` 和 `copyReview.titleRationale` 的当前值与拟写入值。

[事实] 更新 `apps/web/public/styles.css`：

- 新增写回预览面板、字段差异行和状态标签样式。

[事实] 更新 `apps/web/tests/coverAssistantService.test.js`：

- 增加 `buildTitleWritebackPreview` 字段对比测试。
- 增加前端“正式写回预览”可见性断言。

## 当前边界

[事实] 本轮仍未执行真实案例文件写入。

[事实] 预览只说明拟写入：

1. `copyReview.preferredTitle`
2. `copyReview.titleRationale`

[推断] 下一步进入正式写入前，应继续保持“预览 -> 确认 -> 写入 -> 读回 -> 复跑”的受控流程。

## 验证结果

[事实] 已执行定向测试：

```bash
npm test -- --test-name-pattern "TitleWritebackPreview|writeback preview|title selection"
```

结果：2 pass / 0 fail。

[事实] 已执行完整测试：

```bash
npm test
```

结果：289 pass / 0 fail。

[事实] 已执行静态构建：

```bash
npm run build
```

结果：构建通过，产物输出到 `apps/web/dist`。

## 下一步建议

[推断] 下一步适合做“标题选择正式写回受控执行”：

1. 为预览结果增加确认短语或确认按钮状态。
2. 写入对应真实案例 JSON 的 `copyReview`。
3. 写入后读回验证字段一致。
4. 复跑 `real-002 / real-003`，确认人工优选标题稳定进入第一位。

