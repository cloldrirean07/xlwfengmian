# 2026-06-25 关键样例复跑机制接入记录

## 本轮目标

把“规则升级后要重新跑关键样例”这件事，从人工习惯变成正式代码能力。

## 本轮完成

### 1. 新增关键样例复跑计划

新增文件：

- `apps/web/data/operations/key-case-rerun-plan.json`

当前计划包含：

- `sample-001`
- `real-001`

并声明需要顺手刷新的下游目标：

- `reviewed-misclassified`
- `rule-revision-task-sheet`

### 2. 新增批量复跑脚本

新增文件：

- `apps/web/scripts/rerun-key-cases.js`

该脚本会：

1. 读取关键样例计划
2. 复跑关键 case
3. 刷新 case-run 输出
4. 重新生成误判样本导出
5. 重新生成规则修订任务单
6. 生成关键样例复跑报告

### 3. 新增复跑报告能力

新增文件：

- `apps/web/src/domain/cases/buildKeyCaseRerunReport.js`
- `apps/web/src/domain/cases/buildKeyCaseRerunMarkdown.js`
- `apps/web/src/domain/cases/buildObsidianKeyCaseRerunRecord.js`

### 4. 新增命令入口

修改：

- `apps/web/package.json`

新增命令：

- `rerun:key-cases`
- `export:obsidian-key-case-rerun`

### 5. 测试与验证

执行：

- `npm test`
- `npm run rerun:key-cases`

结果：

- 测试通过 80 项
- 成功复跑 `sample-001` 与 `real-001`
- 成功刷新：
  - `reviewed-misclassified`
  - `rule-revision-task-sheet`
- 成功生成：
  - `outputs/reports/key-case-rerun/key-case-rerun.json`
  - `outputs/reports/key-case-rerun/key-case-rerun.md`

## 本轮判断

这轮的意义很实际：

以后每次规则升级，不需要再靠记忆手动去跑：

- `run:case`
- `report:reviewed-misclassified`
- `report:rule-revision-task-sheet`

而是有一条正式维护链路。

## 下一步建议

优先继续：

1. 将高频误判真实案例加入关键复跑清单。
2. 做“复跑前后差异对比报告”。
3. 让关键样例复跑报告也进入更完整的 Obsidian 维护流。
