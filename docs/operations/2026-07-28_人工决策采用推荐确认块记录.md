# 人工决策采用推荐确认块记录

- 时间：2026-07-28 15:52
- 执行模式：PI Engine 维护模式
- 本轮目标：根据人工确认，将批次 `real-002_to_real-003` 的人工决策状态推进为采用推荐确认块。

## 1. 本轮决策

- 更新 `apps/web/outputs/batch-review-manual-safe-write-preview/manual-confirmation-decision/manual-confirmation-decision.md`
  - 将 `决策状态` 从 `pending` 改为 `adopt-recommended`。
  - 将 `决策说明` 从 `待确认` 改为 `采用推荐确认块`。

## 2. 推进含义

- 采用推荐确认块后，可进入安全预览写入前复查。
- 本轮不写入 Obsidian。
- 本轮不执行正式写回。
- 正式写回仍需通过后续安全预览写入与门禁检查。

## 3. 验证口径

- 重新生成采用推荐确认块预演。
- 重新生成人工决策选择索引。
- 运行单元测试、静态构建和界面文案扫描。

## 4. 状态机修正

- 补充人工采用操作包的已应用状态。
- 当决策记录已为 `adopt-recommended` 时，操作包状态显示为 `adoption-packet-applied`。
- 避免已完成的人工采用被误判为操作包未就绪。
