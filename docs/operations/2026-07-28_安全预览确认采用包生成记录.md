# 安全预览确认采用包生成记录

- 时间：2026-07-28 18:10
- 执行模式：PI Engine 维护模式
- 本轮目标：在人工决策已采用后，生成安全预览确认采用包，承接“补安全写回确认”的下一步。

## 1. 背景

- 人工决策状态已为 `adopt-recommended`。
- 正式写回 readiness 当前仍为 `awaiting-safe-write-confirmation`。
- 最近一份安全写回预览缺少人工复盘结论，因此正式写回保持锁定。

## 2. 修正

- 新增 `src/domain/review/buildManualConfirmationSafePreviewAdoptionPacket.js`
  - 汇总建议填写块、人工决策状态、目标安全预览路径和应用后门禁结果。
  - 在建议预演通过且人工决策已采用时输出 `safe-preview-adoption-packet-ready`。
- 新增 `src/application/runManualConfirmationSafePreviewAdoptionPacketStatus.js`
  - 读取项目内预演、人工决策和最近安全写回预览状态。
  - 生成采用包 JSON 与 Markdown。
- 新增 `scripts/generate-manual-confirmation-safe-preview-adoption-packet.js`
  - 提供命令行生成入口。
- 更新 `src/server/server.js`
  - 增加只读接口 `/api/batch-review-manual-confirmation-safe-preview-adoption-packet`。
- 更新 `public/app/api.js`、`public/app/createApp.js`、`public/app/state.js`、`public/app/renderers.js`
  - 将采用包纳入写回门禁证据刷新和页面展示。
- 更新 `tests/coverAssistantService.test.js`
  - 增加采用包生成、阻塞、接口接入和 UI 展示相关断言。

## 3. 生成产物

- JSON：`apps/web/outputs/batch-review-manual-safe-write-preview/manual-confirmation-safe-preview-adoption-packet/manual-confirmation-safe-preview-adoption-packet.json`
- Markdown：`apps/web/outputs/batch-review-manual-safe-write-preview/manual-confirmation-safe-preview-adoption-packet/manual-confirmation-safe-preview-adoption-packet.md`

## 4. 安全边界

- 本轮不写入 Obsidian。
- 本轮不修改最近安全写回预览源文件。
- 本轮不执行正式写回。
- 正式写回仍需在安全写回确认真正写入后重新检查 readiness。
