# 写入前预演接入写回态记录

## 1. 本轮定位

[事实] 本轮继续按 PI Engine 维护模式推进“真实案例数据闭环”。

[事实] 上一轮已经生成项目内写入前预演副本，并验证推荐版本会打开门禁、保守版本会保持锁定。

[事实] 本轮目标是把该预演结果接入前端写回态总览，便于人工确认前直接审阅，不新增写入动作。

## 2. 实现范围

[事实] 新增只读接口：

```http
GET /api/batch-review-manual-confirmation-apply-preview
```

[事实] 前端写回态总览新增“写入前预演”面板，展示：

- 预演结论
- 安全边界
- 推荐填写块合并后的门禁结果
- 保守填写块合并后的门禁结果
- 两份项目内预演副本路径

[事实] 本轮未写入 Obsidian，未执行正式写回，未新增运行时依赖。

## 3. 当前结果

[事实] 推荐确认版本合并后显示为“合并后可写回”。

[事实] 保守确认版本合并后显示为“合并后保持锁定”。

[事实] 页面只展示证据状态，不提供选择版本或执行写入的新增业务操作。

## 4. 待验证项

[事实] 已补充单元测试覆盖源码接入和渲染结果。

[事实] 后续仍需在交付前执行完整测试、构建、文案扫描和本地接口检查。

## 5. 下一步

[需确认] 若继续推进正式写回链路，需要人工确认采用推荐版本，再把人工确认内容写入 Obsidian 安全预览记录。
