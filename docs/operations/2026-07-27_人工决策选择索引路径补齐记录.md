# 人工决策选择索引路径补齐记录

- 时间：2026-07-27 11:25
- 执行模式：PI Engine 维护模式
- 本轮目标：修正人工决策选择索引中的操作包路径展示，避免索引中出现“暂无”。

## 1. 问题

- 采用操作包和暂不采用操作包已成功生成。
- 选择索引脚本读取的操作包 JSON 未包含 `outputPaths`。
- 因此索引 Markdown 中的操作包位置显示为“暂无”。

## 2. 修正

- 更新 `scripts/generate-manual-confirmation-decision-options-index.js`
  - 使用脚本内已知的采用操作包 Markdown 路径补齐索引。
  - 使用脚本内已知的暂不采用操作包 Markdown 路径补齐索引。
- 更新 `src/domain/review/buildManualConfirmationDecisionOptionsIndex.js`
  - 支持显式传入采用操作包和暂不采用操作包路径。
- 更新 `tests/coverAssistantService.test.js`
  - 增加索引 Markdown 中两条操作包路径的断言。

## 3. 安全边界

- 不修改操作包生成逻辑。
- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增服务端接口。
