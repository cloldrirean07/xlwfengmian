# 2026-06-25 真实案例维护优先级看板接入记录

## 本轮目标

在已有真实案例就绪度和关键复跑计划之外，再补一层“真实案例维护优先级看板”。

## 本轮完成

### 1. 新增维护优先级看板领域能力

新增文件：

- `apps/web/src/domain/cases/buildRealCaseMaintenanceBoardReport.js`
- `apps/web/src/domain/cases/buildRealCaseMaintenanceBoardMarkdown.js`
- `apps/web/src/domain/cases/buildObsidianRealCaseMaintenanceBoardRecord.js`

### 2. 新增脚本入口

新增：

- `apps/web/scripts/report-real-case-maintenance-board.js`
- `apps/web/scripts/export-obsidian-real-case-maintenance-board.js`

并接入命令：

- `report:real-case-maintenance-board`
- `export:obsidian-real-case-maintenance-board`

### 3. 看板逻辑

当前看板综合：

- 案例维护优先级
- 维护标签
- 就绪度状态
- 缺失字段
- 建议动作

### 4. 测试与验证

执行：

- `npm test`
- `npm run report:real-case-maintenance-board`

结果：

- 测试 89 项全部通过
- 成功生成真实案例维护优先级看板

### 5. 当前结果

当前 `real-001` 被判断为：

- `P2 / 6`
- 就绪度：待回填
- 当前建议动作：先完成基础字段回填

## 本轮判断

这张看板的意义在于：

后面真实案例一多，你不会只看到“缺了哪些字段”，还会知道“哪条最值得先投入维护”。

这对真实产品推进比单纯的字段检查更接近实际工作方式。

## 下一步建议

优先继续：

1. 把更多真实案例补进来。
2. 让高频误判案例进入这张看板。
3. 后续把人工优先级判断也纳入记录。
