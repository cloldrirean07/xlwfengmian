# 2026-06-20 规则校验与命中解释接入记录

## 本次完成

- 为规则 JSON 增加读取时结构校验
- 增加规则版本元信息生成
- `createAnalysisSession` 返回 `ruleMeta`
- 前端首轮结果页展示命中主方向、匹配分、规则版本、命中信号

## 为什么这一步重要

- 规则库开始变成“可长期维护的产品配置”，而不只是静态文本
- 首轮推荐结果具备了可解释性，更像真实 AI 产品
- 后续做实验和复盘时，可以把规则版本和结果绑定起来

## 当前验证

- 在 `apps/web` 下运行 `npm test`
- 需确认新增测试与原有测试全部通过

## 后续建议

1. 在二轮反馈结果中继续展示命中反馈映射。
2. 给规则版本和实验记录建立统一导出模板。
3. 后续把案例标注反馈词持续回填到 `feedback-catalog.json`。
