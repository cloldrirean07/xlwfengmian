# 2026-06-26 批量真实案例字段校验摘要接入记录

## 本次完成

已在批量真实案例录入链路中接入字段校验摘要。

现在批量预览和批量确认写入结果都能展示：

- 整批案例的 readiness 概览
- 最常缺字段统计
- 每条案例的缺失风险摘要

## 影响文件

### 新增

- `apps/web/src/domain/cases/buildRealCaseBatchValidationSummary.js`
- `docs/architecture/批量真实案例字段校验摘要接入_v0.1.md`

### 修改

- `apps/web/src/application/createRealCaseBatchScaffoldPreview.js`
- `apps/web/src/application/commitRealCaseBatchScaffold.js`
- `apps/web/public/app/renderers.js`
- `apps/web/public/app/createApp.js`
- `apps/web/tests/coverAssistantService.test.js`

## 验证结果

### 自动化验证

已通过：

- `node --check public/app/createApp.js`
- `node --check public/app/renderers.js`
- `npm test`

结果：

- `103 pass / 0 fail`

### 页面验证

发现现有 `3080/3090` 本地页面进程不是本轮最新代码进程：

- `3080` 返回旧接口状态，缺少新 route
- `3090` 页面仍停留在旧运行口结果

因此补开新预览口：

- `http://127.0.0.1:3091`

并在页面中实际验证到：

- 批量预览可正常生成
- 校验摘要可正常显示
- 缺失字段统计与逐条案例风险可见

## 当前结论

这一步已经让“批量录入”从单纯写入动作，升级成带质量提示的工作台动作。  
因此当前更应继续补“推荐动作/回填任务”这一层，而不是马上做视觉型 UI 优化。
