# 2026-06-20 素材类型建议与行动路径记录

## 本次目标

把首轮结果从“系统会解释为什么推荐这个方向”，继续推进到“系统会告诉你现在先做哪一步”。

## 本次完成

### 1. 新增素材类型建议模块

本次新建：

- `src/domain/analysis/inferAssetSuggestion.js`

负责输出：

- `suggestedAssetType`
- `suggestedAssetReason`
- `primaryAssetActionId`
- `primaryAssetActionLabel`
- `primaryAssetActionReason`

### 2. 分析层正式携带执行建议

`extractInputFields` 现在除了内容判断，还会把素材建议和首轮行动路径一起整理出来。

这意味着首轮分析结果已经不仅是“理解内容”，也开始包含“理解用户下一步该怎么动手”。

### 3. 候选图优先顺序新增统一动作标签

`buildRankedImageStrategies` 现在为每个候选路径补了：

- `executionTag`

当前统一为三类：

- `先优化现有图`
- `先补贴内容的图`
- `先做概念图`

### 4. 卡片和总览区开始展示执行性结果

前端首轮结果现在会显示：

- 素材类型建议
- 首轮行动路径
- 每张卡当前最建议的执行路径

这让首轮结果更像真正能指导用户下一步操作的产品结果。

## 当前验证结果

### 语法检查

已通过：

- `node --check src/domain/analysis/inferAssetSuggestion.js`
- `node --check public/app/renderers.js`

### 测试

已通过：

- `npm test`

结果：

- `59 passing`
- `0 failing`

### 接口验证

已通过 `/api/analyze` 本地请求验证，返回中实际出现：

- `suggestedAssetType`
- `suggestedAssetReason`
- `primaryAssetActionLabel`
- `primaryAssetActionReason`
- `executionTag`

说明这层不是只停留在前端显示，而是已经进入 API 结果结构。

## 这次改动后的直接收益

### 1. 首轮结果更可执行

现在系统不只是告诉用户“哪张卡更好”，还会告诉用户：

- 你这次素材更该按什么类型理解
- 你现在先做哪一步最合理

### 2. 为后续流程分支打下基础

以后可以围绕这三个动作标签，逐步拆出不同工作区和不同交互路径。

### 3. 更贴近你要做的专业化产品

这一层就是专业性的一部分：

- 不是泛泛给建议
- 而是给判断和执行路径

## 下一步建议

最优先继续做的是：

1. 根据 `primaryAssetActionLabel` 切出不同的后续工作区
2. 给用户一个“这个素材类型建议不对”的反馈入口
3. 开始让二轮反馈可以针对“先优化现有图”这类动作继续细分
