# 人工采用操作包复制辅助记录

- 时间：2026-07-27 10:12
- 执行模式：PI Engine 维护模式
- 本轮目标：为人工采用操作包增加替换项复制辅助。
- 安全边界：只复制项目内替换项文本，不修改决策记录，不写入 Obsidian，不执行正式写回。

## 1. 实现映射

- 操作包替换项 -> 页面可复制文本
- 复制替换项按钮 -> 剪贴板辅助
- 复制成功或失败 -> 现有状态条反馈
- 自动修改决策记录、写入 Obsidian、正式写回 -> 本轮不实现

## 2. 本轮新增

- 写回态“人工采用操作包”面板新增“复制替换项”按钮。
- 替换项文本以稳定数据标记保留，供复制逻辑读取。
- 复用现有剪贴板函数和状态反馈，不新增写入动作。

## 3. 当前状态

- 当前决策记录仍为：pending
- 操作包状态：adoption-packet-ready
- 是否自动更新决策记录：否
- 是否写入 Obsidian：否
- 是否执行正式写回：否

## 4. 后续条件

人工确认采用后，可复制替换项并手动更新项目内决策记录，再重新生成决策校验与采用预演报告。
