# 正式写回候选批次接入关键样例复跑计划记录

- 记录时间：2026-07-29 17:38
- 项目：AI 封面创意助手
- 本轮目标：继续正式写回后承接计划，把 `real-002_to_real-003` 从文本承诺补齐为关键样例复跑计划中的结构化候选批次。

## 1. 处理边界

- 本轮只更新项目内代码、报告与本地输出。
- 未自动修改规则目录。
- 未写入 Obsidian。
- 未提交 Git。
- 可执行复跑样例仍只使用真实存在的 caseId，当前为 `sample-001` 和 `real-001`。

## 2. 核心调整

- 关键样例复跑计划新增 `formalWriteCandidateBatches` 字段。
- `generate:key-case-rerun-plan` 读取正式写回元数据，并从目标文件名提取 `real-002_to_real-003`。
- 复跑计划 Markdown 增加“正式写回候选批次”段落。
- 正式写回后承接计划读取并展示候选批次。
- 修正承接计划中的已复跑数量读取，使用复跑报告的 `summary.rerunCaseCount`。

## 3. 已执行命令

- `npm run report:rule-revision-task-sheet`
- `npm run generate:key-case-rerun-plan`
- `npm run rerun:key-cases`
- `npm run generate:formal-write-follow-up-plan`

## 4. 当前结果

- 规则修订任务单：已生成，任务数 `1`。
- 关键样例复跑计划：已生成。
- 可执行复跑样例：`sample-001`、`real-001`。
- 正式写回候选批次：`real-002_to_real-003`。
- 关键样例复跑：已完成 `2` 个样例。
- 复跑差异：`0` 个样例变化，下游变化 `0` 项。

## 5. 后续建议

- 若需要继续推进，应先把 `real-002_to_real-003` 拆解为可执行真实样例，再决定是否调整规则引擎。
- 若要沉淀到 Obsidian，应在人工确认后执行对应导出命令。
