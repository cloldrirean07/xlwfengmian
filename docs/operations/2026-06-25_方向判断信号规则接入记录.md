# 2026-06-25 方向判断信号规则接入记录

## 本轮目标

把“5 类封面效果方向”从已有的文案/构图/风险配置，进一步升级成：

- 可结构化维护
- 可解释输出
- 可接前端展示
- 可继续回流到案例与规则修订的方向判断信号系统

## 本轮完成

### 1. 新增方向判断信号规则数据

新增文件：

- `apps/web/data/rules/cover-direction-signal-catalog.json`

覆盖 5 类方向：

- information
- result
- suspense
- conflict
- texture

每类方向都包含：

- `signalGroups`
- `feedbackTriggers`
- `boundaryRules`

### 2. 新增规则加载与校验

修改：

- `apps/web/src/infrastructure/rules/loadRuleCatalog.js`

新增能力：

- 加载方向判断信号规则
- 校验规则结构
- 将 `directionSignalCount` 写入 `ruleMeta`

### 3. 接入领域层与首轮卡片输出

新增：

- `apps/web/src/domain/directions/directionSignalCatalog.js`
- `apps/web/src/domain/directions/buildDirectionSignalChecklist.js`

修改：

- `apps/web/src/domain/analysis/rankDirectionCandidates.js`
- `apps/web/src/domain/cards/buildFirstRoundCards.js`

现在每张方向卡会额外输出：

- `directionSignalChecklist`
- `boundaryRule`

### 4. 前端展示接入

修改：

- `apps/web/public/app/renderers.js`

新增展示：

- 方向判断规则数量
- 当前命中的判断信号
- 固定判断信号清单
- 常见反馈触发词
- 方向边界提醒

### 5. 测试验证

修改：

- `apps/web/tests/coverAssistantService.test.js`

验证点包括：

- 新规则可正常加载
- `ruleMeta.directionSignalCount` 正常输出
- 首轮方向卡带有 `directionSignalChecklist`
- 测试结果全部通过

执行：

`cd /Users/xlw/Documents/codex1/ai-cover-creative-assistant/apps/web && npm test`

结果：

- 77 个测试通过，0 失败

## 本轮判断

这次接入的价值，不只是多加一份 JSON，而是把“方向体系”真正升级成了可维护规则层。

这样后续做以下事情都会更顺：

- 继续补 5 类方向的方法论
- 把案例库和代码输出对齐
- 让前端结果更专业、更像产品顾问
- 为真实模型 Prompt 提供稳定结构

## 下一步建议

优先继续两步：

1. 把方向判断信号接入二轮解释与误判样本链路。
2. 将真实案例/案例库逐步映射到 5 类方向信号下，形成“规则 + 案例”双层系统。
