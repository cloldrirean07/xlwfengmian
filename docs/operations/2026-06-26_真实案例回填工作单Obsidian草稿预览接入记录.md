# 2026-06-26 真实案例回填工作单 Obsidian 草稿预览接入记录

## 本次动作

- 新增 `createRealCaseFillObsidianPreview`
- 更新 `runRealCaseFillPreview`
- 更新 `renderRealCaseMaintenancePreview`
- 补充对应测试

## 本次目的

把“真实案例回填工作单”再往前推进一层，不只展示代码侧 markdown，还展示对应的 Obsidian 草稿预览。

## 本次实现结果

现在从真实案例概览进入单案例维护预览后，可以同时看到：

- 当前最该先补的字段
- 完整回填工作单预览
- Obsidian 草稿建议落点与草稿全文预览

## 为什么这样做

当前阶段更适合先稳定“预览链路”，而不是把页面直接接成真实写盘动作。

这样能先验证：

- Obsidian 草稿结构是否合理
- 页面维护链是否足够清楚
- 代码侧生成内容是否真的能顺利过渡到知识库沉淀

## 验证

- `npm test`
- `node --input-type=module -e "...runRealCaseFillPreview({ caseId: 'real-001' })..."`
- `node --check public/app/renderers.js`

## 当前影响

这次改动没有新增真实写入动作，只新增了更接近最终落地形态的预览层。

后续如果要继续推进，最自然的一步是：

- 在这条预览链路上再补“确认导出到 Obsidian”的显式动作
