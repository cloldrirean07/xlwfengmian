# 2026-06-26 真实案例概览到回填预览联动记录

## 1. 本轮目标

让真实案例概览区可以直接回答：

- 这条案例现在最该先补什么

## 2. 本轮完成内容

### 2.1 新增单条真实案例回填预览用例

新增：

- `src/application/runRealCaseFillPreview.js`

### 2.2 新增服务接口

新增：

- `GET /api/real-case-fill-preview`

### 2.3 新增概览区动作

每条真实案例新增：

- `查看这条先补什么`

### 2.4 新增前端维护预览区

页面会显示：

- 当前就绪度
- 缺失字段数
- 当前最该先补的字段
- 每个字段对应的提示语

## 3. 本轮验证结果

已验证：

- `npm test`
  - `95 pass / 0 fail`
- `node --check public/app/createApp.js`
  - 通过
- `node --check public/app/renderers.js`
  - 通过
- `runRealCaseFillPreview({ caseId: 'real-001' })`
  - 返回：
    - `status: 待回填`
    - `topPriorityItems`
      - `来源链接或截图路径`
      - `内容主题`
      - `素材描述`

## 4. 当前意义

这一步让真实案例概览真正开始承担维护入口作用。

现在它已经可以把单条案例继续导向：

- 同步预览
- 回填优先项预览

## 5. 下一步建议

下一步最顺的是：

1. 把真实案例概览继续接到优先级看板或完整回填工作单
2. 再补更多真实案例内容字段录入
3. 等主链稳定后，再单开 UI 优化讨论
