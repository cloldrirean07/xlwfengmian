# 2026-07-21 规则沉淀交接闭环实现记录

## 1. 执行模式

- 执行方式：PI Engine 维护模式
- 依据文档：
  - `docs/prd/AI封面创意助手_MVP重做_主路径与工作台重做.md`
  - `docs/architecture/AI封面创意助手产品线重做架构计划_v0.1.md`
  - `docs/architecture/AI封面创意助手第三阶段规则沉淀与重构判断_v0.1.md`
- 本轮目标：
  - 承接 PRD 第 5 段后置条件，把复盘看板结果交接到规则修订任务和关键样例复跑
  - 不新增未绑定按钮，不改变导出、写回或规则引擎底层逻辑

## 2. 实现映射

| Spec 条目 | 实现位置 | 实现说明 |
| --- | --- | --- |
| 正式复盘后形成规则修订或关键样例复跑任务 | `apps/web/src/domain/review/buildBatchReviewFollowUpChecklist.js` | 在 UI ready 分支补充规则修订任务单、关键样例复跑两个非按钮交接项。 |
| 规则沉淀与重构判断 | `apps/web/public/app/renderers.js` | 在复盘看板中新增 `Rule Handoff` 展示，根据当前规则判断呈现证据补齐、样本积累或规则修订交接。 |
| UI 信息层级 | `apps/web/public/styles.css` | 增加规则交接面板样式和移动端单列适配。 |
| 回归测试 | `apps/web/tests/coverAssistantService.test.js` | 增加 checklist ready 分支和复盘看板 Rule Handoff 展示测试。 |
| 项目追踪 | `apps/web/updatelog.md` | 记录本轮修改文件、实现边界与验证口径。 |

## 3. 修改边界

本轮未改动：

- 服务端接口
- 前端事件 action id
- 规则引擎数据结构
- Obsidian 导出底层
- 真实案例数据

本轮只修改：

- 复盘后操作清单的 ready 分支说明
- 复盘看板规则交接展示
- 规则交接样式
- 对应测试断言
- 更新日志和操作记录

## 4. 文案自检

本轮新增界面文案均面向产品状态：

- `Rule Handoff`
- `规则修订交接`
- `证据补齐交接`
- `样本积累交接`

本轮没有新增“你、我、我们、这里、这块、刚才”等对话式界面文案。

## 5. 验证口径

本轮应执行：

```bash
node --check public/app/renderers.js
npm test
```

并确认：

- 首页资源可正常返回
- `renderers.js` 包含 `Rule Handoff` 和 `规则修订交接`
- `styles.css` 包含 `rule-handoff-panel`
- `buildBatchReviewFollowUpChecklist` ready 分支包含规则修订任务单和关键样例复跑交接项
