# 准拆页三视图顶部切换定位闭环记录

## 1. 执行时间

- 日期：2026-07-23
- 阶段：PI Engine 维护模式
- 方案：准拆页三视图顶部切换定位补齐

## 2. 执行依据

- `docs/prd/AI封面创意助手_MVP重做_PRD_信息架构确认_v0.1.md`
- `docs/architecture/准拆页三视图实施方案_v0.1.md`
- `docs/operations/2026-07-23_准拆页三视图页头信息架构闭环记录.md`

## 3. 问题判断

三视图已完成显隐、页头和深链恢复，但顶部视图按钮只切换内容，不会自动定位到目标视图页头。长页面内从案例维护或复盘驾驶舱切回其它视图时，视线可能停留在旧滚动位置，影响准拆页的页面感。

## 4. 核心改动

- `apps/web/public/app/createApp.js`
  - 新增产品视图目标选择器函数。
  - 顶部三视图按钮切换后自动定位到目标 `data-product-view` 容器。

- `apps/web/tests/coverAssistantService.test.js`
  - 增加顶部视图切换定位逻辑断言。

## 5. 边界说明

- 未新增业务流程。
- 未新增按钮或权限。
- 未修改深链恢复、工作组展开、案例读取或复盘看板生成逻辑。
- 未引入正式路由。

## 6. 验证结果

- JS 语法检查：通过。
- 现有单元测试：`242 pass / 0 fail`。
- UI 文案扫描：仅命中领域词库中的“你知道吗”，非界面文案。
- 本地服务可达性：首页、`createApp.js` 与 `/#real-case-form` 均返回 `200`。
- 构建脚本：当前 `apps/web/package.json` 未配置 `build` 脚本。
