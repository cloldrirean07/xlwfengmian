# 人工采用操作指引产品化记录

- 时间：2026-07-27 10:37
- 执行模式：PI Engine 维护模式
- 本轮目标：将“接口状态”转译为写回态中的人工采用操作指引。

## 1. Requirement Spec 映射

| 需求边界 | 本轮处理 |
| --- | --- |
| 人工确认前不执行正式写回 | 仅补充页面说明，不修改决策记录 |
| 写回态需要明确下一步 | 在进度总览中加入三步操作指引 |
| 采用推荐确认块后才能进入复查 | 根据当前决策状态展示不同指引 |
| 不新增接口契约 | 复用既有决策、采用预演和操作包状态 |

## 2. 实现范围

- 更新 `public/app/renderers.js`
  - 在“人工采用进度总览”中增加“人工采用操作指引”。
  - `pending` 状态下提示查看预演、复制替换项、人工更新决策记录后刷新门禁。
  - `adopt-recommended` 状态下提示进入安全预览写入前复查。
- 更新 `public/styles.css`
  - 增加操作指引区块样式。
- 更新 `tests/coverAssistantService.test.js`
  - 增加操作指引结构、文案和样式断言。

## 3. 安全边界

- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增服务端接口。
- 不改变 `pending`、`adopt-recommended`、`reject-recommended` 的状态规则。

## 4. 当前状态

- 当前真实决策：pending
- 采用预演：已通过
- 人工采用操作包：可使用
- 下一步仍需人工确认是否采用推荐确认块。
