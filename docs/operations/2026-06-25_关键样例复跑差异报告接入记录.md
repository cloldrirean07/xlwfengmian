# 2026-06-25 关键样例复跑差异报告接入记录

## 本轮目标

在已有关键样例复跑机制上，补一层“前后差异对比”能力。

## 本轮完成

### 1. 新增差异报告领域能力

新增文件：

- `apps/web/src/domain/cases/buildKeyCaseRerunDiffReport.js`
- `apps/web/src/domain/cases/buildKeyCaseRerunDiffMarkdown.js`
- `apps/web/src/domain/cases/buildObsidianKeyCaseRerunDiffRecord.js`

### 2. 扩展复跑脚本

修改：

- `apps/web/scripts/rerun-key-cases.js`

新增逻辑：

- 复跑前读取旧 `result.json`
- 复跑后保留新 `result`
- 对关键字段做 before/after 对比
- 对下游摘要做 before/after 对比
- 输出：
  - `key-case-rerun-diff.json`
  - `key-case-rerun-diff.md`

### 3. 新增 Obsidian 导出脚本

新增：

- `apps/web/scripts/export-obsidian-key-case-rerun-diff.js`

并接入 `package.json` 命令：

- `export:obsidian-key-case-rerun-diff`

### 4. 测试与验证

执行：

- `npm test`
- `npm run rerun:key-cases`

结果：

- 测试 83 项全部通过
- 成功生成复跑差异报告

### 5. 当前 diff 结果

本次再次复跑关键样例后，报告显示：

- `sample-001`：无关键字段变化
- `real-001`：无关键字段变化
- 下游误判样本数：无变化
- 下游规则任务数：无变化

这说明当前规则在关键样例上已经相对稳定，没有继续漂移。

## 本轮判断

这轮的价值不在于“看见变化”，而在于你现在也能清楚地看见“没有变化”。

对于规则系统产品来说，这很重要，因为：

- 没变化意味着当前修改未影响关键样例
- 有变化则意味着需要继续检查是否符合预期

## 下一步建议

优先继续：

1. 把真实高频误判样例加入 diff 清单。
2. 给 diff 结果增加“变化优先级”。
3. 后续让规则升级说明与 diff 结果一起归档。
