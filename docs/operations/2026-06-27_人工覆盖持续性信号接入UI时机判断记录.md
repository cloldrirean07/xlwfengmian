# 2026-06-27_人工覆盖持续性信号接入UI时机判断记录

## 1. 本轮目标

- 让 UI 时机判断不只看样本数量和重复摩擦点。
- 把人工复盘完整度也变成正式信号。

## 2. 本轮完成内容

已完成：

- 跨批次汇总新增人工完整覆盖批次数
- 跨批次汇总新增人工未补齐批次数
- UI readiness 新增“至少 1 批关键人工判断字段补完整”检查项
- UI readiness 风险与下一步动作同步纳入人工复盘完整度
- 前端展示同步补强
- 测试补齐并完成回归验证

## 3. 涉及文件

- `apps/web/src/domain/cases/buildBatchRunFrictionSummaryReport.js`
- `apps/web/src/domain/cases/buildBatchRunFrictionSummaryMarkdown.js`
- `apps/web/src/domain/ui/buildUiOptimizationReadinessReport.js`
- `apps/web/src/domain/ui/buildUiOptimizationReadinessMarkdown.js`
- `apps/web/public/app/renderers.js`
- `apps/web/tests/coverAssistantService.test.js`

## 4. 验证结果

本地测试命令：

- `cd /Users/xlw/Documents/codex1/ai-cover-creative-assistant/apps/web && npm test`

结果：

- `131 pass / 0 fail`

## 5. 当前价值

这轮之后，“还不该做 UI”不再是模糊判断，而能更清楚地说出：

- 是样本不够
- 还是复盘不完整
- 还是重复信号仍不稳定

这会让后续 UI 讨论更像产品判断，而不是凭感觉。

## 6. 下一步建议

下一步优先级建议：

1. 真实补完至少 1 批关键人工字段
2. 再跑 1 批真实内容
3. 重新生成跨批次摩擦点汇总
4. 重新生成 UI 就绪度报告
5. 再决定是否进入 UI 优化专题
