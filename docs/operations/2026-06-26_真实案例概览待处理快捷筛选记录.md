# 2026-06-26 真实案例概览待处理快捷筛选记录

## 本次动作

- 真实案例概览增加 `isActionable`
- 概览筛选增加“只看待处理”
- 补充测试与运行验证

## 本次目的

让真实案例概览更适合真实推进时的高频动作：快速聚焦当前还需要处理的案例。

## 本次实现结果

当前真实案例概览支持：

- 全部
- 各分组
- 只看待处理

## 验证

- `npm test`
- `node --check public/app/renderers.js`
- `node --input-type=module -e "...listAvailableCases()..."`

## 当前影响

这次改动继续强化了“先做高频信息结构优化”的路线。

后续如果继续推进，更自然的方向是：

- 概览页增加默认落在“只看待处理”
- 或直接开始补第 2、3 条真实案例数据
