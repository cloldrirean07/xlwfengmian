# 暂不采用预演与操作包生成记录

- 时间：2026-07-27 11:13
- 执行模式：PI Engine 维护模式
- 本轮目标：为“暂不采用推荐确认块”补齐项目内只读预演和操作包产物，让人工决策两条路径都有可复查证据。

## 1. Requirement Spec 映射

| 需求边界 | 本轮处理 |
| --- | --- |
| 人工决策前不得进入正式写回 | 仅生成预演和操作包，不修改决策记录 |
| 决策状态需可复查 | 复用现有 `reject-recommended` 校验状态生成预演 |
| 不改变状态流转 | 暂不采用后状态为 `decision-rejected`，写回继续锁定 |
| 不新增服务端接口 | 仅新增项目内脚本产物，不接入新的 API |

## 2. 实现范围

- 新增 `src/domain/review/buildManualConfirmationDecisionRejectionPreview.js`
  - 生成暂不采用后的只读预演。
  - 验证暂不采用后不会进入安全预览写入前复查。
- 新增 `src/domain/review/buildManualConfirmationDecisionRejectionPacket.js`
  - 生成暂不采用的两条人工替换项。
  - 输出后续复查说明。
- 新增 `scripts/generate-manual-confirmation-decision-rejection-preview.js`
  - 输出暂不采用预演 JSON 与 Markdown。
- 新增 `scripts/generate-manual-confirmation-decision-rejection-packet.js`
  - 输出暂不采用操作包 JSON 与 Markdown。
- 更新 `package.json`
  - 增加两个 npm 生成命令。
- 更新 `tests/coverAssistantService.test.js`
  - 增加暂不采用预演和操作包测试。

## 3. 安全边界

- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增服务端接口。
- 不改变 `validateManualConfirmationDecision` 的状态判断。

## 4. 当前状态

- 当前真实决策：pending
- 采用路径：预演通过，可进入安全预览写入前复查
- 暂不采用路径：预演通过，正式写回保持锁定
- 下一步仍需人工确认选择。
