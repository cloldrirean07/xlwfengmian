# 2026-06-20 二轮解释验证记录导出记录

## 本次完成

- 新增二轮解释 markdown 构建器
- 新增 Obsidian 二轮解释记录包装器
- 新增 `export:obsidian-refinement-explanation` 脚本
- 导出目录落到 `05_验证与实验/二轮解释验证记录/已生成记录`

## 为什么这一步重要

- 二轮解释进入验证目录，便于后续人工复核
- 可以开始围绕映射 ID 做系统化误判分析
- 让页面解释、运行记录、Obsidian 验证草稿三层开始对齐

## 当前验证

- 在 `apps/web` 下运行 `npm test`
- 需确认新增测试全部通过
- 需运行一次导出脚本，确认 Obsidian 文件实际生成

## 后续建议

1. 建立二轮解释验证记录的批量汇总。
2. 把不合理映射自动推到误判样本导出。
3. 后续接入真实交互记录，不只依赖样例运行结果。
