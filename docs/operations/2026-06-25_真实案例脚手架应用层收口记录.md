# 2026-06-25 真实案例脚手架应用层收口记录

## 1. 本轮目标

把真实案例新增链路从“脚本里直接拼装”推进到“正式应用层可复用能力”。

## 2. 本轮完成内容

### 2.1 新增应用层模块

新增：

- `src/application/prepareRealCaseScaffold.js`
- `src/application/prepareRealCaseBatchScaffold.js`

作用：

- 单条真实案例脚手架准备
- 批量真实案例脚手架准备

### 2.2 收薄脚本层

调整：

- `scripts/scaffold-real-case.js`
- `scripts/scaffold-real-cases-batch.js`

现在脚本层只保留：

- 参数读取
- JSON 读取
- 调用应用层
- 文件写入

### 2.3 修正批量脚本路径实现

统一为：

- `fileURLToPath`
- `dirname`

避免路径实现不一致。

### 2.4 补测试

先新增两条脚手架测试，随后又补两条 API 预览测试，当前总测试数从 `89` 提升到 `93`。

### 2.5 新增最小 API 预览入口

新增：

- `src/infrastructure/cases/loadRealCaseIndex.js`
- `src/application/createRealCaseScaffoldPreview.js`
- `src/application/createRealCaseBatchScaffoldPreview.js`

并在服务层接入：

- `POST /api/real-case-scaffold-preview`
- `POST /api/real-case-batch-scaffold-preview`

当前定位：

- 先做预览
- 暂不直接写文件
- 给后续网页录入或后台录入做统一入口

## 3. 本轮验证结果

已验证：

- `npm test`
  - `93 pass / 0 fail`
- `npm run scaffold:real-case -- --dry-run`
  - 正常输出单条真实案例脚手架
- `npm run scaffold:real-cases-batch -- --dry-run`
  - 正常输出批量脚手架结果

说明：

- 本地新增端口启动在当前沙箱环境里受限，因此本轮 API 入口主要通过应用层测试完成验证。
- 代码层和测试层已证明预览链路成立，后续可在本地正常开发环境里直接连页面使用。

## 4. 当前价值

这轮不是新增用户可见功能，而是补强真实案例维护链的地基。

直接价值：

- 后面继续加 `real-002`、`real-003` 时不容易乱
- 后续如果做后台录入或网页侧录入，可以复用同一套应用层逻辑
- 现在已经有 API 预览入口，后面接页面时不需要再回头改核心结构
- 真实案例新增链更容易补测试和继续扩展

## 5. 下一步建议

下一步最顺的是继续做两件事：

- 用这套脚手架真正新增 `real-002`
- 再往上做一个真实案例录入页或最小内部工作台，直接调用新加的预览 API

这样就会从“可维护脚手架”进一步走到“真实可操作入口”。
