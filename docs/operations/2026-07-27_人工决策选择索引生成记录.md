# 人工决策选择索引生成记录

- 时间：2026-07-27 11:22
- 执行模式：PI Engine 维护模式
- 本轮目标：将采用路径与暂不采用路径的预演、操作包和当前决策状态汇总为一份人工决策选择索引。

## 1. Requirement Spec 映射

| 需求边界 | 本轮处理 |
| --- | --- |
| 人工确认前不执行正式写回 | 索引只读汇总，不修改决策记录 |
| 决策选项需要可比较 | 汇总采用与暂不采用两条路径的预演状态、操作包状态和后果 |
| 不新增服务端接口 | 仅新增项目内脚本产物 |
| 不改变状态流转 | 继续复用现有 `pending`、`adopt-recommended`、`reject-recommended` 状态 |

## 2. 实现范围

- 新增 `src/domain/review/buildManualConfirmationDecisionOptionsIndex.js`
  - 汇总当前决策、采用预演、采用操作包、暂不采用预演和暂不采用操作包。
  - 输出采用和暂不采用的对照结果。
- 新增 `scripts/generate-manual-confirmation-decision-options-index.js`
  - 读取项目内已有 JSON 产物。
  - 输出 `manual-confirmation-decision-options-index.json` 与 `manual-confirmation-decision-options-index.md`。
- 更新 `package.json`
  - 增加 `generate:manual-confirmation-decision-options-index`。
- 更新 `tests/coverAssistantService.test.js`
  - 增加人工决策选择索引测试。

## 3. 安全边界

- 不修改 `manual-confirmation-decision.md`。
- 不写入 Obsidian。
- 不执行正式写回。
- 不新增服务端接口。
- 不替代人工决策。

## 4. 当前状态

- 当前真实决策：pending
- 采用路径：可进入安全预览写入前复查
- 暂不采用路径：正式写回保持锁定
- 索引用途：辅助人工选择下一步。
