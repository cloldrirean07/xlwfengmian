# 人工确认写入前预演闭环记录

## 1. 本轮定位

[事实] 本轮按 PI Engine 维护模式继续推进真实案例数据闭环。

[事实] 当前安全写回链路已经具备人工确认草稿验证，但此前只验证草稿块自身，没有验证草稿合并到安全写回预览副本后的门禁效果。

[事实] 本轮只生成项目内预演副本，不写入 Obsidian，不执行正式写回。

## 2. 本轮新增能力

[事实] 新增脚本：

```bash
npm run generate:manual-confirmation-apply-preview
```

[事实] 脚本会读取：

- `outputs/batch-review-manual-safe-write-preview/batch-review-manual-safe-write-preview.md`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-draft.md`

[事实] 脚本会生成：

- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-apply-preview/manual-confirmation-apply-preview.md`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-apply-preview/manual-confirmation-apply-preview.json`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-apply-preview/suggested-safe-write-preview.md`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-apply-preview/conservative-safe-write-preview.md`

## 3. 门禁结果

[事实] 推荐确认版本合并到安全写回预览副本后，`canProceedToFormalWrite` 为 true。

[事实] 保守确认版本合并到安全写回预览副本后，`canProceedToFormalWrite` 为 false。

[事实] 两个分支均能被 `parseBatchReviewManualSafeWritePreviewNote` 正确解析人工补充字段。

## 4. 已执行验证

[事实] 已执行：

```bash
npm run generate:manual-confirmation-apply-preview
```

[事实] 执行结果：

```json
{
  "ok": true,
  "summary": "人工确认写入前预演通过。建议版本会打开门禁，保守版本会保持门禁锁定。"
}
```

## 5. 下一步建议

[需确认] 如果要继续进入正式写回链路，需要人工确认采用推荐确认版本，并先写入 Obsidian 安全预览记录。

[事实] 在人工确认前，当前项目仍停留在安全预演阶段，不应直接执行正式写回。
