# 2026-06-30_首页三组入口优先级与默认显隐策略接入记录

## 1. 本轮目标

- 把首页三组从“只是分组”继续推进到“有优先级的分组”。
- 让默认展开和默认收起真正服务当前产品推进节奏。

## 2. 本轮完成内容

已完成：

- 为三组增加显式分组标识
- 为三组补展开/收起按钮
- 为组内内容补 `group-body`
- 接入默认显隐初始化策略
- 修正真实案例录入表单 DOM 选择器

## 3. 产出文件

- `docs/architecture/首页三组入口优先级与默认显隐策略接入_v0.1.md`

## 4. 本轮验证

命令：

- `cd /Users/xlw/Documents/codex1/ai-cover-creative-assistant/apps/web && npm test`

结果：

- `143 pass / 0 fail`

## 5. 本轮判断

这轮说明现在的 UI 推进节奏是对的：

- 不是一上来大改视觉
- 而是先把结构边界、组别职责、默认曝光强度收清楚

这样后面再做 UI 讨论时，已经不需要从“这块到底该不该一直露出来”开始争论。

## 6. 下一步建议

下一步最适合继续做的是：

1. 补三组的微说明和使用场景提示
2. 收案例维护组内部的小层级
3. 再决定是否进入首页轻导航或更系统的 UI 优化讨论
