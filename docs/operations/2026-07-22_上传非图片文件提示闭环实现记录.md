# 上传非图片文件提示闭环实现记录

## 基本信息

- 日期：2026-07-22
- 模式：PI Engine 维护模式
- 范围：输入准备区 / 上传图片字段
- 对应文档：[AI封面创意助手_MVP重做_主路径与工作台重做.md](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/docs/prd/AI封面创意助手_MVP重做_主路径与工作台重做.md)

## PRD 映射

- PRD 1.5 上传图片字段要求：仅支持图片文件；单次最多上传 1 张。
- PRD 1.5 异常提示要求：文件类型不支持时，在上传区域提示“仅支持图片文件”，已填文本内容保留。

## 本轮实现

- [public/app/assetPreview.js](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/apps/web/public/app/assetPreview.js)
  - 将非图片文件上传时抛出的错误文案统一为“仅支持图片文件”。
  - 保持图片预览生成、尺寸读取、对象 URL 回收逻辑不变。
- [tests/coverAssistantService.test.js](/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/apps/web/tests/coverAssistantService.test.js)
  - 增加 `buildAssetPreview rejects non-image files with PRD upload message` 测试，覆盖非图片文件被拒绝时的 PRD 指定提示。

## 验收结果

- `node --check apps/web/public/app/assetPreview.js`：通过。
- `node --test --test-reporter=dot`：通过，202 个测试点全部通过。
- `node -p "Boolean(require('./package.json').scripts.build) ? require('./package.json').scripts.build : 'NO_BUILD_SCRIPT'"`：确认当前 web 包未配置 build 脚本。

## 边界说明

- 本轮只处理文件类型不支持的提示一致性，不改变上传入口、预览尺寸读取和清空素材的交互。
- 已填文本内容的保留依赖现有上传失败流程：非图片文件在 `buildAssetPreview` 阶段被拒绝，不触发文本输入重置。
- 单次最多上传 1 张由现有上传控件形态承接，本轮未扩展多文件上传能力。
