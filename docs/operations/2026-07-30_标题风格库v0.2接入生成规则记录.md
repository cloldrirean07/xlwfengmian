# 标题风格库 v0.2 接入生成规则记录

## 本次目标

[事实] 本轮目标是把前面采集的小红书、抖音标题样本从“资料沉淀”推进到“产品规则”，让 `real-002` 美食案例和 `real-003` 晚霞案例在首轮标题建议中，不再只依赖单条写死标题，而是能根据素材关键词命中标题风格库。

## 实现范围

[事实] 新增 `apps/web/src/domain/copy/titleStyleLibrary.js`，沉淀标题风格库 v0.2。

[事实] 新增 `apps/web/data/title-library/title-style-library.v0.2.json`，记录标题风格、适用平台、适用场景、触发词、候选标题和样本来源。

[事实] 更新 `apps/web/src/domain/copy/materialKeywordCopy.js`，将真实素材关键词接入标题风格库，输出顺序调整为：

1. 人工优选标题
2. 标题风格库候选标题
3. 情绪标题种子
4. 素材关键词兜底标题

[事实] 更新 `apps/web/src/domain/analysis/extractInputFields.js`，补齐 `copyReview` 字段透传，使真实案例中的人工优选标题能进入生成链路。

[事实] 更新 `apps/web/src/domain/cards/buildFirstRoundCards.js`，首轮标题建议从 2 条扩展到 3 条，便于展示不同风格路径。

[事实] 更新 `apps/web/tests/coverAssistantService.test.js`，增加标题风格库与人工优选标题优先级测试。

## real-002 复跑观察

[事实] `real-002` 首轮标题建议已从泛化教程标题，推进为更贴近素材的美食标题：

1. 在家复刻夜市香辣鱿鱼
2. 香辣蟹做出饭店味道
3. 香辣蟹新手零失败

[推断] 这说明“螃蟹、香辣鱿鱼、辣炒、香菜”等素材词已能把案例导向美食分享/夜市复刻/低门槛教程风格，而不是只停留在 AI 封面教程话术。

## real-003 复跑观察

[事实] `real-003` 首轮标题建议已从泛化教程标题，推进为更贴近素材的晚霞标题：

1. 一键还原落日余晖感
2. AI把晚霞做成封面大片
3. 适合晚霞封面的朋友圈文案

[推断] 这说明“晚霞、落日、霞光、云层、朋友圈文案、调色”等素材词已能把案例导向风景效果、AI 方法论和文案合集风格。

## 验证结果

[事实] 已执行定向测试：

```bash
npm test -- --test-name-pattern "title style library|manual preferred"
```

结果：2 pass / 0 fail。

[事实] 已执行完整测试：

```bash
npm test
```

结果：285 pass / 0 fail。

[事实] 已执行静态构建：

```bash
npm run build
```

结果：构建通过，产物输出到 `apps/web/dist`。

[事实] 已执行关键样例复跑：

```bash
npm run rerun:key-cases
```

结果：plannedCaseCount 4，rerunCaseCount 4，realCaseCount 3。

[事实] 已执行真实案例就绪度与维护看板：

```bash
npm run report:real-case-readiness
npm run report:real-case-maintenance-board
```

结果：真实案例 3 个，ready 2 个，pending 1 个，高优先级就绪案例 2 个。

## 下一步建议

[推断] 下一步不宜继续只追加标题，而应进入“标题风格库产品化展示”：

1. 在前端首轮结果卡中展示标题来源：人工优选、风格库、情绪标题、素材词兜底。
2. 为标题候选增加风格标签，例如“夜市复刻”“AI 风景效果”“文案合集”。
3. 将用户选择的标题回流到 `copyReview`，形成“案例输入 -> 候选生成 -> 人工选择 -> 规则复盘”的产品闭环。

