# 人工确认交接包生成记录

## 1. 本轮定位

[事实] 本轮继续按 PI Engine 维护模式推进真实案例数据闭环。

[事实] 上一轮已经完成写入前预演接入写回态页面，但正式写回仍需要人工确认。

[事实] 本轮生成项目内人工确认交接包，用于把“推荐确认块、目标记录、写入后复查命令”收束到一份可审阅文件中。

[事实] 本轮不写入 Obsidian，不执行正式写回。

## 2. 新增脚本

[事实] 新增命令：

```bash
npm run generate:manual-confirmation-handoff-packet
```

[事实] 脚本读取：

- `outputs/batch-review-manual-safe-write-preview/batch-review-manual-safe-write-preview.md`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-draft.md`

[事实] 脚本输出：

- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-handoff-packet/manual-confirmation-handoff-packet.md`
- `outputs/batch-review-manual-safe-write-preview/manual-confirmation-handoff-packet/manual-confirmation-handoff-packet.json`

## 3. 交接包内容

[事实] 交接包包含：

- 目标批次
- 目标记录
- 当前改写来源
- 推荐确认块
- 推荐版本写入后的门禁预期
- 保守版本写入后的门禁预期
- 写入后复查命令

[事实] 当前交接包状态为 `ready-for-manual-transfer`。

[事实] 推荐版本写入后预期为“可打开门禁”。

[事实] 保守版本写入后预期为“保持锁定”。

## 4. 已执行验证

[事实] 已执行：

```bash
npm run generate:manual-confirmation-handoff-packet
```

[事实] 执行结果：

```json
{
  "ok": true,
  "status": "ready-for-manual-transfer",
  "summary": "人工确认交接包已生成，可用于人工写入安全预览记录。"
}
```

## 5. 下一步

[需确认] 若继续推进正式写回链路，需要人工确认采用交接包中的推荐确认块，并将该块写入 Obsidian 安全预览记录底部。

[事实] 写入 Obsidian 安全预览记录后，应重新检查正式写回门禁；只有门禁进入 `ready-to-formal-write` 后，才进入正式写回确认。
