# 2026-07-12 代码模块地图与路径校准记录

## 1. 本轮目标

这轮不是新增一条全新业务链路，而是做两类基础收口：

1. 把项目根路径的真实位置校准清楚
2. 把当前 `apps/web` 已经形成的代码模块地图补成文档

这样后面继续开发与讨论 UI 时，不会再建立在旧路径和模糊结构上。

## 2. 本轮确认到的工程事实

- 当前真实项目根目录：
  `/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant`
- 当前真实 Web 应用目录：
  `/Users/xlw/Documents/codex1/AI封面创意助手项目/ai-cover-creative-assistant/apps/web`
- `src` 已明确分为：
  - `application`
  - `domain`
  - `infrastructure`
  - `server`
  - `shared`

## 3. 本轮发现的问题

存在一批历史文档与运行产物仍引用旧根路径：

- `/Users/xlw/Documents/codex1/ai-cover-creative-assistant`

这类残留分两种：

### 3.1 需要优先纠正的

- 根目录说明文档
- 项目架构规范
- 更新日志中的错误文件链接

### 3.2 可以后续通过重生成刷新掉的

- `outputs/` 下历史产物中的旧路径
- 旧批次导出记录里嵌入的绝对路径

这部分不建议手工全量替换，因为它们本质上属于历史运行快照。

## 4. 本轮已处理内容

- 修正仓库 README 中的真实根路径
- 修正《项目目录与代码架构规范 v0.1》中的根路径
- 修正 `apps/web/updatelog.md` 中错误的测试文件链接
- 新增《当前 Web 应用代码模块地图 v0.1》

## 5. 对后续 UI 的影响判断

当前已经具备讨论 UI 的前提材料，但还不适合直接做系统级重写。

更准确地说：

- 可以继续做轻量 UI 收口
- 还不应该立刻做大面积视觉翻修

原因是：

- `review` 链路仍然较重
- `cases` 与 `workspace` 仍在继续收口
- 规则层和第二轮优化层还会继续演进

## 6. 下一步建议

优先继续做下面两类工作：

1. 规则与方向层继续稳定
   - `domain/directions`
   - `domain/effects`
   - `domain/refinement`
2. 案例与复盘闭环继续稳定
   - `domain/cases`
   - `domain/review`
   - `application`

等这两段再稳一点，再单独开一轮系统 UI 方案讨论会更合适。
