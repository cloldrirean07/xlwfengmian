# 2026-06-21 人工复核写回与目录规范推进记录

## 本轮目标

- 修复二轮解释验证记录的人工复核字段写回问题。
- 打通“误判样本导出 -> 规则修订任务单”的下游链路。
- 明确 `ai-cover-creative-assistant` 的总目录与代码分层规范。

## 本轮完成

### 1. 修复人工复核写回工具

涉及文件：

- `apps/web/src/domain/refinement/applyRefinementExplanationReviewFields.js`
- `apps/web/tests/coverAssistantService.test.js`

修复内容：

- 修正字段替换正则，避免跨行吞掉后续字段。
- 将写回逻辑升级为“缺字段时自动补齐”。
- 新增测试，覆盖“字段缺失也能恢复”的情况。

### 2. 验证测试状态

执行：

`cd /Users/xlw/Documents/codex1/ai-cover-creative-assistant/apps/web && npm test`

结果：

- 77 个测试全部通过。

### 3. 回写样例复核记录

已对以下记录重新写回完整结构化字段：

- `sample-001`

当前已能在记录内看到：

- `review_status`
- `explanation_status`
- `misclassified`
- `should_export_to_misclassified`
- `suggested_keyword`
- `suggested_mapping_id`
- `suggested_positive_signal_id`

### 4. 验证误判样本导出链路

通过单独调试确认：

- `parseRefinementExplanationReviewNote`
- `buildReviewedMisclassifiedExportReport`

对 `sample-001` 已能产出：

- `eligibleExportCount = 1`

说明业务逻辑已经打通，后续可继续执行导出同步。

### 5. 固化目录与代码分层规范

新增文档：

- `docs/architecture/项目目录与代码架构规范_v0.1.md`

明确内容包括：

- 项目根目录统一使用 `ai-cover-creative-assistant`
- 顶层目录职责
- `apps/web` 的分层方式
- 新增代码/规则/报告的放置规则
- 与 Obsidian 的协作边界

## 当前判断

这轮不是简单“补脚本”，而是在为后续真实产品开发建立一条稳定链路：

- 文档可规划
- 规则可结构化
- 实验可复核
- 误判可回流
- 代码可持续扩展

## 下一步建议

优先继续三件事：

1. 将 reviewed misclassified 与 rule revision task sheet 正式导出到 Obsidian。
2. 为 5 类封面效果方向补“判断信号清单”结构。
3. 开始把第一版网页输入输出流程继续做成更完整的页面与接口边界。
