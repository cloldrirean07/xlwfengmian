# 规则任务到关键样例复跑承接闭环记录

- 日期：2026-07-23
- 阶段：PI Engine 维护模式 / 第三阶段
- 对应 Requirement Spec：案例复盘与规则沉淀 / 批次复盘看板

## 本轮目标

在复盘驾驶舱中补齐规则任务后的下一步验证链路：

```text
规则修订任务信号 -> 关键样例复跑候选 -> 下游刷新目标
```

## 事实基线

- 项目已有关键样例复跑能力：
  - `npm run generate:key-case-rerun-plan`
  - `npm run export:obsidian-key-case-rerun-plan`
  - `npm run rerun:key-cases`
  - `npm run export:obsidian-key-case-rerun`
  - `npm run export:obsidian-key-case-rerun-diff`
- 项目已有规则修订任务单：
  - `REV-001`
  - 关联样本：`sample-001`
  - 建议映射：`neg-content-distance`
- `rerun:key-cases` 已会刷新：
  - `reviewed-misclassified`
  - `rule-revision-task-sheet`

## 实现内容

1. 在复盘看板报告中新增 `keyCaseRerunHandoff`。
2. 从 `ruleRevisionSignal.topTasks[].caseIds` 聚合关键样例候选。
3. 在 Markdown 复盘看板中新增“关键样例复跑承接”章节。
4. 在前端复盘驾驶舱中展示：
   - 当前状态
   - 候选样例
   - 下游刷新目标
   - 建议命令顺序

## 边界

- 不在浏览器中新增执行复跑脚本的按钮。
- 不修改关键样例复跑脚本。
- 不修改规则词库。
- 不改变正式写回流程。

## 验收口径

- 有规则修订任务时，看板显示“可生成关键样例复跑计划”。
- 关联样本 `sample-001` 进入候选样例。
- 看板显示复跑命令顺序。
- Markdown、页面渲染和报告层测试全部覆盖。
