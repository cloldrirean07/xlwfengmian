# 2026-08-10_第二轮规则修订与结果卡收束记录

- 生成时间：2026-08-10 23:12
- 本轮目标：清理缓存漂移后，继续修订 `real-002` / `real-003` 暴露的规则问题，并把首页方向卡改成更适合创作者判断的结果卡。

## 1. 本轮处理的问题

[事实] 第一轮修订后，`real-002` 和 `real-003` 已从 B 级提升到 A-，但仍有两个明显问题：

1. `real-003` 晚霞天空素材在真实输入下，第一轮主方向仍容易被平台权重推向“更让人想点开”，没有稳定前置“更高级专业”。
2. `real-002` 美食强冲击素材的配图策略仍可能沿用“左右对照 / 前后对照”的通用冲突话术。

[推断] 这说明当前规则已经能识别素材关键词，但还需要把“素材场景”进一步影响方向排序、配图建议和结果卡解释。

## 2. 实现范围

### 2.1 缓存清理

[事实] 本轮先清理工作区缓存：

- 还原 4 个 `apps/web/outputs/batch-*` 缓存漂移文件。
- 移除未跟踪的 `outputs/docx-qa/` 临时质检输出目录。

### 2.2 规则修订

[事实] 本轮新增两类素材场景识别：

- `food-impact`：螃蟹、辣炒鱿鱼、海鲜、红油、辣椒、美食、夜宵、下饭等信号。
- `sunset-sky`：晚霞、霞光、落日、云层、天空、风景等信号。

[事实] 规则修订后：

- 美食强冲击素材会更稳定进入“更有冲击力”。
- 晚霞天空素材会更稳定进入“更高级专业”。
- 美食配图策略改为放大食物近景、红油、海鲜和辣椒。
- 晚霞配图策略改为保留天空留白、云层方向、落日色彩和低干扰标题区。

### 2.3 首页结果卡

[事实] 首页方向卡新增面向创作者的解释字段：

- 为什么值得先试
- 视觉重点
- 标题策略
- 构图动作

[推断] 这一步的价值是把后台规则判断翻译成用户可行动的封面执行建议，让用户不必展开完整判断依据，也能知道下一步怎么做。

## 3. 复跑结果

### 3.1 `real-002`

[事实] 复跑后首轮主方向：

```text
更有冲击力 / 强冲突感
```

[事实] 标题候选：

```text
辣炒的味蕾
辣炒海鲜封面，先抓住这一口
红油和海鲜，是这张封面的点击点
```

[事实] 配图与构图建议：

```text
优先放大食物近景、红油、海鲜和辣椒，让食欲冲击点成为封面主视觉。
食物主体占画面中心，标题避开蟹壳、鱿鱼和红油高光。
```

### 3.2 `real-003`

[事实] 复跑后首轮主方向：

```text
更高级专业 / 强质感感
```

[事实] 标题候选：

```text
最后一抹霞光
最后一抹霞光，适合这样做封面
一张天空图也能做出封面感
```

[事实] 配图与构图建议：

```text
优先保留天空留白、云层方向和落日色彩，把标题放进低干扰区域。
标题放在低干扰天空区，地平线和暗部稳定画面。
```

## 4. 验证结果

[事实] 已执行完整测试：

```text
297 pass / 0 fail
```

[事实] 已执行静态构建：

```text
Built AI封面创意助手 static bundle: public -> dist
```

[事实] 已复跑：

```text
real-002: ok
real-003: ok
```

## 5. 当前判断

[推断] 当前 MVP 已进入第二阶段：不只是能生成方向卡，而是能把小而精样本转化为稳定规则，并在首页结果卡中给出创作者可读的行动解释。

[推断] 下一步适合做“结果卡真实预览验证”：让用户打开本地页面，用 `real-002` / `real-003` 从真实案例入口加载，检查是否能在 30 秒内判断哪张卡最值得继续优化。
