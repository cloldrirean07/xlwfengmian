# 2026-08-10_第一轮规则修订后复跑记录

- 生成时间：2026-08-10
- 关联任务单：`2026-08-10_第一轮规则修订任务单.md`
- 复跑案例：`real-002`、`real-003`
- 运行命令：

```bash
/Users/xlw/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node --test
/Users/xlw/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/build-static.mjs
/Users/xlw/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node apps/web/scripts/run-case.js --case-id real-002
/Users/xlw/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node apps/web/scripts/run-case.js --case-id real-003
```

## 1. 验证结果

[事实] 完整测试通过：

```text
296 pass / 0 fail
```

[事实] 静态构建通过：

```text
Built AI封面创意助手 static bundle: public -> dist
```

## 2. `real-002` 复跑结果

### 2.1 第一轮标题

[事实] 第一轮标题候选已补入美食冲击标题：

```text
辣炒的味蕾
辣炒海鲜封面，先抓住这一口
红油和海鲜，是这张封面的点击点
```

### 2.2 第二轮融合

[事实] 第二轮不再输出“辣炒的味蕾，但更贴内容”，而是输出：

```text
辣炒的味蕾，封面先抓住这一口
辣炒海鲜封面，先抓住这一口
红油和海鲜，是这张封面的点击点
```

[事实] 二轮封面大字已保留混合策略：

```text
辣炒鱿鱼、螃蟹、香菜封面先看这一步 / 重点更清楚，食欲冲击别丢
```

[事实] 配图和构图建议已补入素材细节：

```text
裁切时优先放大红油、海鲜、辣椒和食物近景，保留一眼有食欲的冲击点。
标题服务食物主体，避免遮挡红油、蟹壳、鱿鱼和辣椒。
```

### 2.3 判定

[推断] `real-002` 从 B 级提升到 A-：标题、构图和二轮融合已明显贴近 `mvp-ref-005` / `mvp-ref-008` 的标准答案。剩余可优化点是方向卡 A 的配图方向仍沿用“强对照”通用模板，后续可再做美食专属配图策略。

## 3. `real-003` 复跑结果

### 3.1 第一轮标题

[事实] 第一轮标题候选已补入晚霞任务标题：

```text
最后一抹霞光
最后一抹霞光，适合这样做封面
一张天空图也能做出封面感
```

### 3.2 第二轮融合

[事实] 第二轮不再输出“最后一抹霞光，但更贴内容”，而是输出：

```text
最后一抹霞光，适合这样做封面
一张天空图也能做出封面感
夏日晚霞照别只发原图
```

[事实] 二轮封面大字已保留混合策略：

```text
夏日晚霞、云层、落日封面先看这一步 / 重点更清楚，天空情绪别丢
```

[事实] 配图和构图建议已补入天空策略：

```text
裁切时保留天空留白、云层方向和落日色彩，把标题放在低干扰区域。
用天空留白承接短标题，地平线或暗部区域负责稳定画面。
```

### 3.3 判定

[推断] `real-003` 从 B 级提升到 A-：标题已能自然结合“最后一抹霞光”和封面教程任务，构图建议也接入 `mvp-ref-014` 的天空留白标准。剩余可优化点是第一轮方向仍没有显式给出“更高级专业”，后续可再修订风景类方向排序。

## 4. 本轮规则修订结论

[事实] 本轮已实现：

1. 美食标题机制：`具体食材 + 食欲冲击 + 教程任务`。
2. 晚霞标题机制：`情绪记忆点 + 封面教程任务`。
3. 二轮方向融合：`清楚重点 + 食欲冲击` / `清楚重点 + 天空情绪`。

[推断] 当前 MVP 已从“能跑流程”进入“能根据精标样本改善输出”的阶段。

## 5. 后续建议

下一轮优先级：

1. 修订风景类方向排序，让 `mvp-ref-014` 对应的“更高级专业”进入第一轮候选。
2. 修订美食强冲击配图策略，减少“左右对照/前后对照”的通用表达。
3. 把规则修订和样本来源在主工作台中做轻量解释，让用户知道建议从哪里来。

